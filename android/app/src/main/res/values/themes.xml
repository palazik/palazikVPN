<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base XML theme — Compose handles all real theming.
         android:Theme.Material.Light.NoActionBar is the correct parent for
         a minSdk 26 app using Compose without AppCompat. -->
    <style name="Theme.palazikVPN" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
