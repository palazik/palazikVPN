name: Linux CI

on:
  push:
    branches:
      - master
    paths:
      - 'linux/**'
      - '.github/workflows/linux.yml'
  workflow_dispatch:

jobs:
  build:
    name: Build Linux app
    runs-on: ubuntu-latest
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      # Linux builds go to the maintainer's DM, not the group (same as iOS). The bot
      # can only DM this user if they have pressed Start on the bot at least once.
      PM_CHAT_ID: "6939377982"
    defaults:
      run:
        working-directory: linux
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── Java ────────────────────────────────────────────────────────────────
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # The wrapper jar is never committed — fetch the one matching
      # gradle/wrapper/gradle-wrapper.properties (9.4.1).
      - name: Download gradle-wrapper.jar
        run: |
          curl -fL --retry 3 -o gradle/wrapper/gradle-wrapper.jar \
            https://raw.githubusercontent.com/gradle/gradle/v9.4.1/gradle/wrapper/gradle-wrapper.jar

      # ── Runtime assets bundled into the app (resources/linux-x64) ───────────
      # Geo files are the same .dat sources Android and iOS use.
      - name: Download geo files
        run: |
          mkdir -p resources/linux-x64
          curl -fL --retry 3 -o resources/linux-x64/geoip.dat \
            https://github.com/v2fly/geoip/releases/latest/download/geoip.dat
          curl -fL --retry 3 -o resources/linux-x64/geosite.dat \
            https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geosite.dat

      - name: Download xray core
        run: |
          curl -fL --retry 3 -o /tmp/xray.zip \
            https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip
          unzip -o /tmp/xray.zip -d /tmp/xray
          install -m 755 /tmp/xray/xray resources/linux-x64/xray
          resources/linux-x64/xray version

      - name: Download tun2socks
        run: |
          curl -fL --retry 3 -o /tmp/tun2socks.zip \
            https://github.com/xjasonlyu/tun2socks/releases/latest/download/tun2socks-linux-amd64.zip
          unzip -o /tmp/tun2socks.zip -d /tmp/tun2socks
          install -m 755 "$(find /tmp/tun2socks -type f -name 'tun2socks*' | head -1)" \
            resources/linux-x64/tun2socks

      # ── Build ────────────────────────────────────────────────────────────────
      - name: Build distributable
        run: ./gradlew --no-daemon createReleaseDistributable

      - name: Package tar.gz
        run: |
          APP_DIR=$(find build/compose/binaries -maxdepth 3 -type d -name "palazikVPN" | head -1)
          if [ -z "$APP_DIR" ]; then echo "❌ No app image produced"; exit 1; fi
          tar -C "$(dirname "$APP_DIR")" -czf palazikVPN-linux-x64.tar.gz "$(basename "$APP_DIR")"
          ls -lh palazikVPN-linux-x64.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: palazikVPN-linux-x64
          path: linux/palazikVPN-linux-x64.tar.gz
          retention-days: 30

      # Telegram bots cap uploads at 50 MB — a bundled-JRE app image usually exceeds
      # that, so the archive is split into 48 MB parts when needed
      # (reassemble with: cat palazikVPN-linux-x64.tar.gz.part* > palazikVPN-linux-x64.tar.gz).
      - name: Send build to Telegram PM
        if: ${{ env.TELEGRAM_BOT_TOKEN != '' }}
        run: |
          send_doc() {
            local FILE="$1" CAPTION="$2"
            RESP=$(curl -s -X POST \
              "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
              -F "chat_id=${PM_CHAT_ID}" \
              -F "document=@${FILE}" \
              -F "caption=${CAPTION}" \
              -F "parse_mode=Markdown")
            if echo "$RESP" | grep -q '"ok":true'; then
              echo "✓ Sent ${FILE}"
            else
              echo "❌ Telegram failed (did you press Start on the bot?) — $RESP"
              exit 1
            fi
          }

          SHORT_SHA="${GITHUB_SHA:0:7}"
          FILE=palazikVPN-linux-x64.tar.gz
          SIZE=$(stat -c%s "$FILE")
          LIMIT=$((49 * 1024 * 1024))

          if [ "$SIZE" -le "$LIMIT" ]; then
            send_doc "$FILE" "*palazikVPN* — Linux x64
          Commit: \`${SHORT_SHA}\`
          Extract and run \`bin/palazikVPN\` (see README for Arch install)."
          else
            split -b 48M -d "$FILE" "${FILE}.part"
            PARTS=$(ls ${FILE}.part* | wc -l)
            i=1
            for PART in ${FILE}.part*; do
              send_doc "$PART" "*palazikVPN* — Linux x64 (part ${i}/${PARTS})
          Commit: \`${SHORT_SHA}\`
          Reassemble: \`cat ${FILE}.part* > ${FILE}\`"
              i=$((i + 1))
            done
          fi
